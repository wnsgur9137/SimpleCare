Tuist 프로젝트를 생성하고 빌드 검증을 수행합니다.

다음 단계를 순서대로 실행하세요:

1. `tuist install` 실행하여 SPM 의존성 다운로드
2. `tuist generate` 실행하여 Xcode 프로젝트 생성
3. `fastlane ios build_test` 실행하여 DEV 설정으로 컴파일 확인
4. 빌드 결과를 요약하여 보고 (성공/실패, 경고 수, 에러 내용)

빌드 실패 시:
- 에러 메시지를 분석하고 원인을 파악
- 가능한 경우 수정 방안 제시
